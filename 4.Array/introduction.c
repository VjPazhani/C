/* Array is the collection of similar data types stored in common name

Array is the single variable that stores multiple values

Array have two types :
    1.One Dimensional array.
    2.Two Dimensional array.
    
     */
//Program for static 1d Dimmensional array
#include<stdio.h>
int main(){
    int a[5]={1,2,3},i;
    for(i=0;i<=5;i++){
    printf("%d\n",a[i]);
    
    }
}
//--------------------See the output for clear understanding------------------------------